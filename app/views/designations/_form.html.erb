<%= form_for(@designation) do |f| %>
  <% if @designation.errors.any? %>
    <div id="error_exdesignationation">
      <h2><%= pluralize(@designation.errors.count, "error") %> prohibited this designation from being saved:</h2>

      <ul>
      <% @designation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
      <%= f.label :Name %>
      <%= f.text_field :name, class: "form-control", autofocus: true, placeholder: "Enter Name", required: true %>
  </div>
  <br/>
  <%= f.submit class:"btn btn-info btn-fix"  %>
<%end%>