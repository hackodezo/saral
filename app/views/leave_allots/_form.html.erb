<%= form_for(@leave_allot) do |f| %>
  <% if @leave_allot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leave_allot.errors.count, "error") %> prohibited this allot_leafe from being saved:</h2>

      <ul>
      <% @leave_allot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
    <div class="col-md-6">
      <div class="field">
        <%= f.label :employee_id %><br>
        <%=f.select :employee_id, Employee.all.map { |u| [u.name, u.id] },
                                                { include_blank: true },
                                                { class: "chosen-select" }%>
      </div>
    </div>
    <div class="col-md-6">
      <div class="field">
        <%= f.label :Leave_Structure %><br>
        <%= f.select :leave_structure_id, LeaveStructure.all.map { |u| [u.name, u.id] },
                                                { include_blank: true },
                                                { class: "chosen-select" } %>
      </div>
    </div>
  </div>
  <br/>
  <div class="row">
    <div class="col-md-4"></div>
    <div class="col-md-4">
      <div class="actions">
        <%= f.submit class:"btn btn-primary"%>
      </div>
    </div>
  </div>
<% end %>
