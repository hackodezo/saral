<%= form_for(@leave_head) do |f| %>
  <% if @leave_head.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leave_head.errors.count, "error") %> prohibited this leave_head from being saved:</h2>

      <ul>
      <% @leave_head.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control", autofocus: true, placeholder: "Enter Head Name" %>
  </div>
  <br/>
  <div class="actions">
    <%= f.submit class:"btn btn-primary"%>
  </div>
<% end %>
